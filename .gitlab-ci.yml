stages:
  - test
  - build
  - deploy

test:freebsd:
  stage: test
  tags:
    - freebsd
    - jdk8
  script:
    - ./gradlew check

build:all:
  stage: build
  tags:
    - jdk8
  script:
    - ./gradlew build

pages:
  stage: deploy
  only:
    - tags
  tags:
    - dl
  script:
    - ./gradlew uploadArchives

